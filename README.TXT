For unit test:
	Call make in the project directory
	and run the generated executables:
		resize_unittest
		sparse-array_unittest
		robinHoodTest
		linearProbeTest

For benchmark demo:
	Go inside the benchmark directory,
	call make, and run the executable benchmark.

For the demo program:
	Go inside the benchmark directory,
	call make, and run the executable userInterface.


